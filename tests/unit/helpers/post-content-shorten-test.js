
import { postContentShorten } from 'blogging/helpers/post-content-shorten';
import { module, test } from 'qunit';

module('Unit | Helper | post content shorten');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postContentShorten([42]);
  assert.ok(result);
});

